Requirements:
        MinGW (If doesn't compile try downloading version 11.0 instead of latest version).
        SFML 2.5.1
How to install:
        1. Clone this repository: https://github.com/uraniumcrystalsmaster/Potential-Alternatives-to-std-map
        2. Change command lines directory to repository’s directory
        3. run: git submodule update --init --recursive
How to use:
        1. Open the repository's directory folder as new project in IDE.
        2. Press the compile button and it should start.
        3. Labels provide instructions.
